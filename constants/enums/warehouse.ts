//ENUM Denoting all the fields that are suitable/reusable in the filter query
export enum WarehouseEnum {
	"warehouse_name" = "warehouse_name",
	"is_group" = "is_group",
	"parent_warehouse" = "parent_warehouse",
}
