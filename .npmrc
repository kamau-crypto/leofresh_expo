# Flatten node_modules so autolinking + metro can resolve packages
shamefully-hoist=true

# Ensure peer deps are installed automatically
auto-install-peers=true

# Prevent pnpm from using isolated node_modules (causes RN packager issues)
node-linker=hoisted
